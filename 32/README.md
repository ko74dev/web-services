# FastAPI Web Application with Jinja2 Templates

Веб-приложение на FastAPI с рендерингом HTML-шаблонов через Jinja2.

## Особенности

- Эндпоинт `GET /`, который рендерит HTML-шаблон через Jinja2
- HTML-шаблон `index.html` выводит список задач циклом
- Конфигурационный файл `.env.example` для настроек приложения

## Установка и запуск

1. Установите зависимости:
   ```bash
   pip install fastapi uvicorn jinja2
   ```

2. Запустите приложение с помощью uvicorn:
   ```bash
   uvicorn main:app --host 127.0.0.1 --port 8000 --reload
   ```

3. Откройте в браузере:
   - `http://localhost:8000/`
   - или `http://127.0.0.1:8000/`

## Структура проекта

```
.
├── main.py              # Основной файл приложения FastAPI
├── templates/
│   └── index.html       # HTML-шаблон с циклом для вывода задач
├── .env.example         # Пример файла настроек
├── README.md            # Документация проекта
```

## Описание эндпоинтов

- `GET /` — Главная страница, рендерит `index.html` с списком задач

## Пример списка задач

В приложении используется следующий список задач по умолчанию:
```python
tasks_db = [
    {"id": 1, "title": "Создать проект FastAPI", "description": "Инициализировать проект", "completed": True},
    {"id": 2, "title": "Добавить шаблоны Jinja2", "description": "Настроить рендеринг HTML", "completed": True},
    {"id": 3, "title": "Создать страницу задач", "description": "Отобразить список задач", "completed": False}
]
```

## Технические детали

- Приложение использует `jinja2.Environment` с `FileSystemLoader` для рендеринга шаблонов
- Кэширование шаблонов отключено для корректной работы с динамическими данными
- Сервер запускается через `uvicorn` с поддержкой автоперезагрузки при изменениях кода
